# Railway Configuration — Natural OS Backend
# Sin healthcheck HTTP — Railway verifica que el proceso esté vivo

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
